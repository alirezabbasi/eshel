# Workspace Ruleset

This repository now has a split architecture:

- `echel-core/` is the Echel platform/framework source of truth.
- Generated software projects should live outside `echel-core/` (default: `projects/`).

If there is any conflict, follow `echel-core/ruleset.md` as the platform authority.
